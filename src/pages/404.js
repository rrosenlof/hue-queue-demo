import React from "react"
import '../../static/styles.css'
import Layout from "../components/layout"

const NotFound = () => {

        return (
            <Layout>
                <h2>Page Not Found</h2>
                <p>Sorry, try somewhere else...</p>
            </Layout>
        )
}

export default NotFound